<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <rotate android:drawable="@drawable/ic_border_ring" />

        <!-- in case you have a bitmap instead of a SVG, you can use the following instead -->
        <!--rotate
            <bitmap
                android:src="@drawable/ic_border_ring"
                android:gravity="center" />
        </rotate>-->
    </item>

    <item
        android:drawable="@drawable/ic_pixlogo"
        android:top="80dp"
        android:bottom="80dp"
        android:left="80dp"
        android:right="80dp" />
</layer-list>

